name = "resumeiq"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

[build]
command = "npm run build"

[build.upload]
format = "service-worker"

# Environment variables
[vars]
NODE_ENV = "production"

# Optional: Custom domains
# [[routes]]
# pattern = "resumeiq.com/*"
# zone_id = "your-zone-id"
